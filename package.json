{
  "name": "kagizine",
  "version": "1.0.0",
  "private": true,
  "description": "Page-turning magazine reader for the Kagi News daily feeds",
  "scripts": {
    "dev": "wrangler dev",
    "predev": "node scripts/sync-viewer.mjs",
    "start": "wrangler dev",
    "prestart": "node scripts/sync-viewer.mjs",
    "deploy": "wrangler deploy",
    "predeploy": "node scripts/sync-viewer.mjs",
    "test": "vitest run",
    "pretest": "node scripts/sync-viewer.mjs",
    "test:watch": "vitest",
    "typecheck": "tsc --noEmit",
    "types": "wrangler types",
    "viewer:sync": "node scripts/sync-viewer.mjs",
    "viewer:update": "git submodule update --remote vendor/magazine-web-viewer && node scripts/sync-viewer.mjs",
    "tail": "wrangler tail kagizine --format pretty"
  },
  "devDependencies": {
    "@cloudflare/vitest-pool-workers": "^0.21.0",
    "@cloudflare/workers-types": "^5.20260810.1",
    "typescript": "^7.0.2",
    "vitest": "^4.1.10",
    "wrangler": "^4.120.0"
  },
  "dependencies": {
    "fast-xml-parser": "^5.10.1"
  }
}
