{
  "name": "kagizine",
  "version": "1.0.0",
  "private": true,
  "description": "Page-turning magazine reader for the Kagi News daily feeds",
  "scripts": {
    "dev": "uv run pywrangler dev",
    "start": "uv run pywrangler dev",
    "deploy": "uv run pywrangler deploy",
    "test": "uv run pytest -q",
    "tail": "npx wrangler tail kagizine --format pretty"
  },
  "devDependencies": {
    "wrangler": "^4.120.0"
  }
}
