# Toolchains
node_modules/

# Wrangler's local state, including the simulated R2 bucket `pywrangler dev`
# builds editions into.
.wrangler/
.dev.vars

.DS_Store

# package-lock.json is deliberately NOT ignored: this is a deployed
# application, and it should build from pinned dependencies rather than from
# whatever resolved on the machine that ran the deploy.

# Artifacts of the retired local fetch, superseded by R2.
/assets/
/store/
/data.js
/fetch-report.json
/logs/

